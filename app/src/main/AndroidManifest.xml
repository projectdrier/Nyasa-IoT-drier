<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.chiya.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".meatstopped"></activity>
        <activity android:name=".meatdrying" />
        <activity android:name=".meat" />
        <activity android:name=".fruitstopped" />
        <activity android:name=".fruitdrying" />
        <activity android:name=".fruit" />
        <activity android:name=".vegstopped" />
        <activity android:name=".vegdrying" />
        <activity android:name=".veg" />
        <activity android:name=".fishstopped" />
        <activity android:name=".fishdrying" />
        <activity android:name=".fish" />
        <activity
            android:name=".first"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity2" />

        <service android:name=".java.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>